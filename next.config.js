/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/jl-surveying-app',
    images: {
      unoptimized: true,
    },
  }
  
  module.exports = nextConfig